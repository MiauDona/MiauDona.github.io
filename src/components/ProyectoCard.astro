---
import Background from "./Background.astro";
interface Props {
    title: string;
    text: string;
    text2?: string;
    text3?: string;
    text4?: string;
    link?: string;
    img?: string;
}
const title = Astro.props.title;
const text = Astro.props.text;
const text2 = Astro.props.text2;
const text3 = Astro.props.text3;
const text4 = Astro.props.text4;
const link = Astro.props.link;
const img = Astro.props.img;
---

<div class="card jaini pb-4">
    <Background tx="120%" position="relative" o=".4"/>

    <div class="title pb-2">
        <h3 class="text-3xl">{title}</h3>
    </div>
    <div class="grid grid-cols-12 gap-x-2">
        <div class="text col-span-12 sm:col-span-9">
            <p class="text-md jaini">{text}</p>
            {text2 && <p class="text-md jaini">{text2}</p>}
            {text3 && <p class="text-md jaini">{text3}</p>}
            {text4 && <p class="text-md jaini">{text4}</p>}
            {link && <a class="p-1 pl-0 w-full" href={link}>
                    Échale un vistazo
            </a>}
        </div>
        <div class="col-span-12 sm:col-span-3"></div>


    </div>


</div>


<style>
    .card {
        background: transparent;
        height: max-content;
        width: 100%;
        border-radius: 16px;
    }
</style>